---
title: مقدمة إلى بايثون
description: تعلم أساسيات بايثون والصيغة الأساسية
---

## ما هي بايثون؟

بايثون هي لغة برمجة عالية المستوى ومفسرة، معروفة ببساطتها وسهولة قراءتها. إنها مثالية للمبتدئين وقوية بما يكفي للمحترفين. تستخدم بايثون المسافات البادئة لتعريف كتل الكود، مما يجعلها نظيفة وسهلة القراءة.

## المتغيرات وأنواع البيانات

في بايثون، لا تحتاج إلى التصريح عن أنواع المتغيرات بشكل صريح. تحدد بايثون النوع تلقائياً بناءً على القيمة المخصصة.

```python
# الأرقام
age = 25
price = 19.99

# النصوص
name = "Alice"
message = 'Hello, World!'

# القيم المنطقية
is_active = True
has_permission = False

# القوائم (المصفوفات)
numbers = [1, 2, 3, 4, 5]
fruits = ["apple", "banana", "orange"]

# القواميس (أزواج المفتاح والقيمة)
person = {
    "name": "John",
    "age": 30,
    "city": "New York"
}
```

## العمليات الأساسية

```python
# العمليات الحسابية
result = 10 + 5      # الجمع: 15
result = 10 - 5      # الطرح: 5
result = 10 * 5      # الضرب: 50
result = 10 / 5      # القسمة: 2.0
result = 10 // 3     # القسمة الصحيحة: 3
result = 10 % 3      # الباقي: 1
result = 2 ** 3      # الأس: 8

# عمليات النصوص
greeting = "Hello" + " " + "World"  # الدمج
repeated = "Hi" * 3                  # التكرار: "HiHiHi"
```

## الجمل الشرطية

تستخدم بايثون `if` و `elif` و `else` للمنطق الشرطي. تذكر أن تضع مسافة بادئة لكتل الكود!

```python
age = 18

if age < 13:
    print("You are a child")
elif age < 18:
    print("You are a teenager")
else:
    print("You are an adult")

# عوامل المقارنة
x = 10
if x > 5 and x < 15:
    print("x is between 5 and 15")

# التحقق من وجود قيمة
fruits = ["apple", "banana", "orange"]
if "apple" in fruits:
    print("Apple is in the list")
```

## الحلقات

### حلقات For

```python
# التكرار عبر قائمة
fruits = ["apple", "banana", "orange"]
for fruit in fruits:
    print(fruit)

# التكرار عبر نطاق من الأرقام
for i in range(5):
    print(i)  # يطبع 0, 1, 2, 3, 4

# التكرار مع بداية ونهاية
for i in range(1, 6):
    print(i)  # يطبع 1, 2, 3, 4, 5

# التكرار مع خطوة
for i in range(0, 10, 2):
    print(i)  # يطبع 0, 2, 4, 6, 8
```

### حلقات While

```python
count = 0
while count < 5:
    print(count)
    count += 1  # زيادة بمقدار 1

# Break و continue
i = 0
while i < 10:
    i += 1
    if i == 3:
        continue  # تخطي هذا التكرار
    if i == 7:
        break     # الخروج من الحلقة
    print(i)
```

## الدوال

تساعدك الدوال على تنظيم الكود وإعادة استخدامه. استخدم الكلمة المفتاحية `def` لتعريف دالة.

```python
# دالة بسيطة
def greet():
    print("Hello!")

greet()  # استدعاء الدالة

# دالة مع معاملات
def greet_person(name):
    print(f"Hello, {name}!")

greet_person("Alice")

# دالة مع قيمة إرجاع
def add(a, b):
    return a + b

result = add(5, 3)  # result = 8

# دالة مع معاملات افتراضية
def greet_with_title(name, title="Mr."):
    return f"Hello, {title} {name}"

print(greet_with_title("Smith"))              # Hello, Mr. Smith
print(greet_with_title("Jones", "Dr."))       # Hello, Dr. Jones
```

## الإدخال والإخراج

```python
# الطباعة إلى وحدة التحكم
print("Hello, World!")
print("Age:", 25)
print(f"My name is {name} and I am {age} years old")  # f-strings

# الحصول على إدخال المستخدم
name = input("What is your name? ")
age = input("How old are you? ")
age = int(age)  # تحويل النص إلى عدد صحيح
```

## القوائم والعمليات الأساسية

```python
# إنشاء قائمة
numbers = [1, 2, 3, 4, 5]

# الوصول إلى العناصر
first = numbers[0]      # العنصر الأول: 1
last = numbers[-1]      # العنصر الأخير: 5

# إضافة عناصر
numbers.append(6)       # إضافة إلى النهاية: [1, 2, 3, 4, 5, 6]
numbers.insert(0, 0)    # إدراج في موضع: [0, 1, 2, 3, 4, 5, 6]

# إزالة عناصر
numbers.remove(3)       # إزالة القيمة 3
last = numbers.pop()    # إزالة وإرجاع العنصر الأخير

# طول القائمة
length = len(numbers)   # الحصول على عدد العناصر

# التقطيع
subset = numbers[1:4]   # الحصول على العناصر من الفهرس 1 إلى 3
```

## التعليقات

```python
# هذا تعليق من سطر واحد

"""
هذا تعليق متعدد الأسطر
أو docstring. يمكن أن يمتد
عبر عدة أسطر.
"""

# استخدم التعليقات لشرح الكود الخاص بك
def calculate_area(radius):
    # حساب مساحة الدائرة
    pi = 3.14159
    return pi * radius ** 2
```

## ما التالي؟

الآن بعد أن فهمت الصيغة الأساسية لبايثون، يمكنك البدء في بناء برامج بسيطة! مارس كتابة الكود، وجرب أمثلة مختلفة، وانتقل تدريجياً إلى موضوعات أكثر تقدماً مثل:

- البرمجة الكائنية (Classes و Objects)
- التعامل مع الملفات
- معالجة الاستثناءات
- الوحدات والحزم
- العمل مع المكتبات

برمجة سعيدة! 🐍
